<UserControl x:Class="LibreLinkConnector.Views.WidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="280" d:DesignWidth="280">
    <StackPanel Width="240" Margin="12">
        <!-- Widget Header -->
        <Grid Margin="0,0,0,12">
            <TextBlock Text="Blood Glucose" 
                       Style="{StaticResource BodyTextStyle}"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="🔄" 
                        Style="{StaticResource IconButtonStyle}"
                        Padding="8"
                        FontSize="14"
                        Click="RefreshButton_Click"
                        Margin="0,0,4,0"
                        ToolTip="Refresh Data"/>
                <Button Content="⬜" 
                        Style="{StaticResource IconButtonStyle}"
                        Padding="8"
                        FontSize="18"
                        FontWeight="Bold"
                        Click="ExpandButton_Click"
                        ToolTip="Expand to Full View (or double-click anywhere)"/>
            </StackPanel>
        </Grid>
        
        <!-- Large Value Display -->
        <StackPanel HorizontalAlignment="Center" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="{Binding CurrentValueText}" 
                           Foreground="{DynamicResource TextBrush}"
                           FontSize="64"
                           FontWeight="Light"/>
                <TextBlock Text="{Binding UnitText}"
                           FontSize="18"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Bottom"
                           Margin="8,0,0,12"/>
            </StackPanel>
            
            <!-- Compact Status Badge -->
            <Border Background="{Binding StatusBrush}"
                    CornerRadius="6"
                    Padding="12,6"
                    HorizontalAlignment="Center"
                    Margin="0,12,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding TrendArrowText}" 
                               FontSize="18"
                               Foreground="White"
                               Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding StatusText}" 
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="White"/>
                </StackPanel>
            </Border>
        </StackPanel>
        
        <!-- Compact Info -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding PatientNameText}" 
                           Style="{StaticResource BodyTextStyle}"
                           FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding LastUpdateText}" 
                           Style="{StaticResource CaptionTextStyle}"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
