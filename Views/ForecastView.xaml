<UserControl x:Class="LibreLinkConnector.Views.ForecastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <!-- Current Reading Card - Hero Section -->
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <TextBlock Text="Current Glucose" 
                                   Style="{StaticResource SubtitleTextStyle}"/>
                        <Button Content="🔄 Refresh" 
                                HorizontalAlignment="Right"
                                Style="{StaticResource IconButtonStyle}"
                                Click="RefreshButton_Click"/>
                    </Grid>
                    
                    <!-- Main Value Display -->
                    <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,24">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding CurrentValueText}" 
                                       Foreground="{Binding CurrentValueBrush}"
                                       Style="{StaticResource LargeDisplayTextStyle}"/>
                            <StackPanel VerticalAlignment="Bottom" Margin="12,0,0,12">
                                <TextBlock Text="{Binding UnitText}"
                                           FontSize="20"
                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        
                        <!-- Trend and Status -->
                        <Border Style="{StaticResource StatusBadgeStyle}"
                                Background="{Binding StatusBrush}"
                                Margin="0,16,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding TrendArrowText}" 
                                           FontSize="24"
                                           Foreground="White"
                                           Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding StatusText}" 
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           Foreground="White"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    
                    <!-- Info Grid -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Target High -->
                        <Border Grid.Column="0" 
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource SuccessBrush}"
                                BorderThickness="2"
                                CornerRadius="8"
                                Padding="16,12"
                                Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="Target High" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding TargetHighText}" 
                                           Style="{StaticResource BodyTextStyle}"
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Target Low -->
                        <Border Grid.Column="1" 
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource WarningBrush}"
                                BorderThickness="2"
                                CornerRadius="8"
                                Padding="16,12"
                                Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="Target Low" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding TargetLowText}" 
                                           Style="{StaticResource BodyTextStyle}"
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Patient -->
                        <Border Grid.Column="2" 
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="2"
                                CornerRadius="8"
                                Padding="16,12">
                            <StackPanel>
                                <TextBlock Text="Patient" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding PatientNameText}" 
                                           Style="{StaticResource BodyTextStyle}"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <!-- History Card -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Recent History" 
                               Style="{StaticResource SubtitleTextStyle}"
                               Margin="0,0,0,20"/>
                    
                    <ListView ItemsSource="{Binding HistoryItems}" 
                              MaxHeight="350"
                              BorderThickness="0"
                              Background="Transparent">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="12,16"/>
                                <Setter Property="Margin" Value="0,0,0,8"/>
                                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" 
                                               Text="{Binding Timestamp}" 
                                               Style="{StaticResource BodyTextStyle}"
                                               VerticalAlignment="Center"/>
                                    
                                    <StackPanel Grid.Column="1" 
                                                Orientation="Horizontal" 
                                                Margin="20,0">
                                        <TextBlock Text="{Binding Value}" 
                                                   FontSize="24"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text=" mg/dL" 
                                                   Style="{StaticResource SecondaryTextStyle}"
                                                   VerticalAlignment="Center"
                                                   Margin="4,2,0,0"/>
                                    </StackPanel>
                                    
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding TrendArrowSymbol}" 
                                               FontSize="24"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Status Footer -->
            <Border Background="{DynamicResource CardBrush}"
                    BorderBrush="{DynamicResource AccentBrush}"
                    BorderThickness="0,2,0,0"
                    CornerRadius="8"
                    Padding="20,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Ellipse Grid.Column="0" 
                             Width="8" Height="8" 
                             Fill="{DynamicResource SuccessBrush}"
                             Margin="0,0,12,0"
                             VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding AutoUpdateStatusText}" 
                               Grid.Column="1"
                               Style="{StaticResource BodyTextStyle}"
                               TextWrapping="Wrap"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
